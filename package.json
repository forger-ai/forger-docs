{
  "name": "forger-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared bilingual technical documentation for Forger Desktop and the public Forger site.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "generate": "node scripts/generate.mjs",
    "export": "node scripts/export-docs.mjs",
    "build": "npm run validate && npm run generate",
    "check": "npm run build && node scripts/check-generated.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
